## Public API Report File for "@spike-rabbit/element-ng_localization"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { BehaviorSubject } from 'rxjs';
import { Direction } from '@angular/cdk/bidi';
import { Directionality } from '@angular/cdk/bidi';
import { EventEmitter } from '@angular/core';
import * as i0 from '@angular/core';
import { InjectionToken } from '@angular/core';
import { Observable } from 'rxjs';
import { ReplaySubject } from 'rxjs';

// @public (undocumented)
export const SI_LOCALE_CONFIG: InjectionToken<SiLocaleConfig>;

// @public (undocumented)
export const SI_LOCALE_LOCAL_STORAGE_KEY = "lang";

// @public (undocumented)
export const SI_LOCALE_STORE: InjectionToken<string>;

// @public (undocumented)
export class SiDefaultLocaleStore extends SiLocaleStore {
    constructor(isBrowser: boolean);
    // (undocumented)
    get locale(): string | undefined;
    // (undocumented)
    saveLocale(locale: string): Observable<boolean>;
}

// @public
export class SiDirectionality implements Directionality {
    // (undocumented)
    readonly change: EventEmitter<Direction>;
    // (undocumented)
    get value(): Direction;
    // (undocumented)
    readonly valueSignal: i0.WritableSignal<Direction>;
}

// @public (undocumented)
export interface SiLocaleConfig {
    availableLocales?: string[];
    defaultLocale?: string;
    dynamicLanguageChange?: boolean;
    fallbackEnabled?: boolean;
    localeInitializer?: (localeId: string) => Promise<any>;
}

// @public
export class SiLocaleId extends String {
    constructor();
    // (undocumented)
    toLowerCase(): string;
    toString(): string;
    // (undocumented)
    valueOf(): string;
}

// @public (undocumented)
export class SiLocaleService {
    constructor();
    config: SiLocaleConfig;
    hasLocale(locale?: string): boolean;
    readonly locale$: BehaviorSubject<string>;
    set locale(value: string);
    // (undocumented)
    get locale(): string;
    readonly localePackageLoaded$: ReplaySubject<void>;
}

// @public
export abstract class SiLocaleStore {
    // (undocumented)
    abstract get locale(): string | undefined;
    // (undocumented)
    abstract saveLocale(locale: string): Observable<boolean>;
}

// (No @packageDocumentation comment for this package)

```
